extends layout
block content
 h1(style='margin-left:70px') welcome
 center
   a(href="/next",style='color:black;')
     button.form-control(type='submit' name='submit') Go
     each data, i in title
       p #{data.name}
